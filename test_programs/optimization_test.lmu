class RemoveClass {
    int size = 0;

    method getSize() {
        return this.size;
    }

}

class RemoveMethods {
    int color = 0;
    int number = 0;
    int size = 0;

    method getSize() {
        return this.size;
    }

    method changeColor( color ) {
        while (this.color < color) do{
            this.color = this.color + 1;
        }
    }

    method count_down( n ) {
        if (n== 0)then{
            return 1;
        }
        else{
            print(n);
            return this.count_down(n-1) +1;
        }
    }
}

function example( n ) {
    if( n == 1 ) then{
        return n;
    }

    return n * example(n - 1);
}

inst RemoveMethods m1;
inst RemoveClass r1;
int a = 0;
int b = 0;

b = input();
m1.changeColor( 5);

print(1);

return;

print(555+1+a);
