class Test {
    attr int a = 1;
    attr int b = 2;

    method beta ( arg1 ) {
        var int a = 6;


        if ( 1==1+1) then {
            a = arg1;
            a = 3;
        } else {
            a = 2;  #  a = 2
        }
    }
}

function beta ( argx ) {
    var int x = 1;

    x = 2;

    x = 1;

    beta(3);

}

var Test testing;
var int a = 2;

beta(1);

testing.beta(testing.beta(2));
